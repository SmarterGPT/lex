---
"@smartergpt/lex": patch
---

Return a closeable HTTP server handle and reject bind failures. Keep OAuth state local to each router and expire it on requests without an import-time background timer.
